<?xml version="1.0" encoding="UTF-8"?>
<!--
    New changelog (P6) — no recorded history is at risk here, unlike the
    versioned 1.0.0/ changesets. See docs/aiae-migration-plan.md §2.7 for why
    this file lives directly under db/changelog/changes/ rather than under
    1.0.0/, and .claude/agent_docs/distributed_cache.md for the outbox this
    table backs.
-->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="0003-create-cache-invalidation-event" author="aionboarding">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="cache_invalidation_event"/>
            </not>
        </preConditions>
        <createTable tableName="cache_invalidation_event">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="tracked_class" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <createIndex
                tableName="cache_invalidation_event"
                indexName="idx_cache_invalidation_event_created_at">
            <column name="created_at"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
